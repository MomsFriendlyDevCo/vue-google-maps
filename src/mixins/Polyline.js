import Path from './Path';

export default {
	mixins: [Path],
	props: {
		/*
		smoothFactor: {
			type: Number,
			custom: true,
			default: 1.0
		},
		noClip: {
			type: Boolean,
			custom: true,
			default: false
		}
		*/
	}
};
